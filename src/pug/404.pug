-
  var pageTitle = '404 - Page Not Found | SACCK';
  var pageDescription = '';

doctype html
html(lang="ja")
  head
    include ./include/_head
  body

    include ./include/_svg

    div.l-wrapper

      include ./include/_header

      include ./include/_sidebar

      // main
      main.l-main
        .l-container
          .hero
            h2.heading.heading--1 404 - Page Not Found
            p.hero__text アクセスしようとしたページが存在しません
            p.hero__text
              a.link(href="index.html")
                svg(
                  width="13"
                  height="13"
                )
                  use(
                    xlink:href="#back" fill="currentColor"
                  )
                | Back to top

      include ./include/_footer
