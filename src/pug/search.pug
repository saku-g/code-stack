-
  var pageTitle = '検索結果：{words} | SACCK';
  var pageDescription = '';

doctype html
html(lang="ja")
  head
    include ./include/_head
  body

    include ./include/_svg

    div.l-wrapper

      include ./include/_header

      include ./include/_sidebar

      //main
      main.l-main
        .l-container
          .hero
            h2.heading.heading--1 「test」の検索結果
            p.hero__text X件の検索結果を表示。（検索結果がありません。）
          .cards
            - for (var i = 0; i < 9; i++)
              include ./include/_card

      include ./include/_footer
